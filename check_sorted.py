#!/usr/bin/python3
# -*- coding: utf-8 -*-

def check_sorted(A, ascending = True):
	"""
	Проверка отсортированности массива за O(len(A))
	"""
	flag = True
	s = 2*int(ascenfing) - 1
	for i in range(0,N-1):
		if s*A[i] > s*A[i+1]
		flag = false
		break
	return flag

